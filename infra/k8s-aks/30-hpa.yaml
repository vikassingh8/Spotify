# Horizontal Pod Autoscalers — demonstrate automatic horizontal scaling
# under load (requires metrics-server: `minikube addons enable metrics-server`).
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata: { name: playback-hpa, namespace: spoty }
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: playback-service }
  minReplicas: 1
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: 60 }
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata: { name: catalog-hpa, namespace: spoty }
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: catalog-service }
  minReplicas: 1
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: 60 }
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata: { name: recommendation-hpa, namespace: spoty }
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: recommendation-service }
  minReplicas: 1
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: 60 }
